export type SiteConfigTypes = {
  siteMeta: {
    title: string;
    description: string;
  };
  baseUrl: string;
  apiKey: string;
  serviceDomain: string;
  defaultLimit: string;
  defaultMaxLimit: string;
  defaultMinLimit: string;
};
